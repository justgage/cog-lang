# this is a simple add function, pretty useless
func add l r = l + r;

func subtract = add(l, (- r));

display(2, " plus ", 2, " is ", add(2,2));

display("I can have as many as I want");

display("Statements that is");
